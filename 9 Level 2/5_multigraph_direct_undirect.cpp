#include <bits/stdc++.h>

using namespace std;

int main() {
    // Direct, undirect doesn't matter, but there will be multiple edges

    // in adjacent matrix
    // in unweighted graph, if i get 3, then it means there are 3 edges
    // a[1][3] = 3; // 3 edges exist
    // a[1][2] = 0; // no edges or can't go

    // in weighted multi graph, we can't use adjacent matrix


    return 0;
}